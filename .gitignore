node_modules/
dist/
*.log

# Claude Code local permission allowlist (machine-specific)
.claude/settings.local.json

# Local output from `npm run plan`
.plan-out.json

# Demo projects generated while trying things out. `_` marks them as scratch,
# and they are real vibesquad projects, so without this they arrive as a folder
# of someone else's app inside this repository.
_vibesquad_*/

# Screenshots, recordings and drafts made for writing about this, not for
# shipping it.
vibesquad-shots/
*.gif
*.zip
velog-*.md
